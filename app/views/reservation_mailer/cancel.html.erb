<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Hola!</h1>
    <p>
      Tu reserva #<%= @reservation.id %> con <strong><%= "#{@worker.name}" %></strong>
      el día <strong><%= @work_day.name %> <%=@reservation.day.strftime("%Y-%m-%d") %></strong> a las
       <strong><%= @block_time.start.strftime("%I:%M %p") %> - <%= @block_time.end.strftime("%I:%M %p") %></strong> ha sido cancelada! 
       <br>
    </p>
    <p>¡Gracias por confiar en nosotros!</p>
  </body>
</html>